{
  "name": "inside-timer",
  "version": "1.0.0",
  "description": "Inside Timer is a timer that allows you to interact with your inner self",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "debug": "DEBUG=* eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0"
  },
  "dependencies": {
    "eleventy-plugin-i18n": "^0.1.3"
  }
}